# URL base da API do backend (loja-discos-api)
# Para apontar para outro host (ex.: IP fixo em produção), sobrescreva
# criando um .env.local com o novo valor.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
